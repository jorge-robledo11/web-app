---
description: Explica un concepto o nodo específico del grafo de Graphify
agent: build
---

Explica este concepto del grafo de conocimiento:

```text
$ARGUMENTS
```

Ejecuta desde la raíz del repo:

```bash
graphify explain "$ARGUMENTS"
```

Usa la salida para explicar el concepto y sus conexiones principales. Si el grafo no existe, indica que primero debe ejecutarse `/graphify .`.
