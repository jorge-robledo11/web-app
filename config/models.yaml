# Modelos livianos recomendados para agentes OpenCode.
#
# OpenCode no importa este archivo automáticamente en frontmatter.
# Úsalo como catálogo de referencia para copiar el valor en `model:` cuando quieras
# fijar un modelo específico en un agente.

opencode_go:
  minimal: opencode-go/mimo-v2.5
  flash: opencode-go/deepseek-v4-flash

usage:
  changelog: opencode_go.flash
  improve-commits: opencode_go.flash